<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
        <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
            <div class="flex w-full justify-end">
                <a href="/logout" class="bg-red-500 rounded-md px-2 py-2 text-sm mb-4 inline-block">Logout</a>
            </div>
            <h5 class="mb-5">Edit your post <%=post.user.name%></h5>
            <form action="/update/<%=post._id%>" method="POST" class="">
                <textarea class="p-3 mb-3 outline-none w-1/2 bg-transparent resize-none border-2 border-zinc-800 rounded-md block" name="newPost" required><%=post.content%></textarea>
                <input type="submit" class="bg-yellow-600 px-4 py-2 rounded-md block" value="Update">   
            </form>
           
        </div>
</body>
</html>